// 1. CINEMATIC LOADING SCREEN SCRIPT (Diperbaiki agar langsung jalan)
function startLoading() {
    const loadingBar = document.getElementById('loading-bar');
    const loadingText = document.getElementById('loading-text');
    const loadingScreen = document.getElementById('loading-screen');
    
    if (!loadingBar || !loadingScreen) return;

    let progress = 0;
    const interval = setInterval(() => {
        progress += Math.floor(Math.random() * 12) + 6;
        if (progress > 100) progress = 100;
        
        loadingBar.style.width = progress + '%';
        if (loadingText) loadingText.innerText = progress + '%';
        
        if (progress === 100) {
            clearInterval(interval);
            setTimeout(() => {
                loadingScreen.style.opacity = '0';
                setTimeout(() => {
                    loadingScreen.style.display = 'none';
                    initAIAssistant(); // Panggil AI Assistant setelah loading selesai
                }, 800);
            }, 300);
        }
    }, 60);
}

// Jalankan loading langsung saat DOM siap (tidak perlu menunggu gambar eksternal)
if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', startLoading);
} else {
    startLoading();
}

// 2. NEON CURSOR TRAIL (Hanya aktif di desktop/perangkat dengan mouse)
const cursorDot = document.getElementById('cursor-dot');
const cursorOutline = document.getElementById('cursor-outline');

if (cursorDot && cursorOutline) {
    window.addEventListener('mousemove', (e) => {
        const posX = e.clientX;
        const posY = e.clientY;

        cursorDot.style.left = `${posX}px`;
        cursorDot.style.top = `${posY}px`;

        cursorOutline.animate({
            left: `${posX}px`,
            top: `${posY}px`
        }, { duration: 200, fill: "forwards" });
    });
}

// 3. AI HOLOGRAM ASSISTANT TYPING EFFECT
const aiMessages = [
    "Selamat datang di Command Center!",
    "Semua sistem utama beroperasi dengan normal.",
    "Silakan periksa live terminal & aset digital kamu.",
    "Sistem AI Lab siap mendukung eksperimen."
];

function initAIAssistant() {
    const aiTyping = document.getElementById('ai-typing');
    if (!aiTyping) return;

    let msgIndex = 0;
    let charIndex = 0;

    function typeNextChar() {
        const currentMsg = aiMessages[msgIndex];
        
        if (charIndex < currentMsg.length) {
            aiTyping.innerHTML += currentMsg.charAt(charIndex);
            charIndex++;
            setTimeout(typeNextChar, 40);
        } else {
            setTimeout(() => {
                aiTyping.innerHTML = '';
                charIndex = 0;
                msgIndex = (msgIndex + 1) % aiMessages.length;
                typeNextChar();
            }, 3500);
        }
    }
    typeNextChar();
}

function toggleAI() {
    const aiBox = document.getElementById('ai-assistant');
    if (aiBox) aiBox.style.display = 'none';
}

// 4. MATRIX / PARTICLE CANVAS BACKGROUND
const canvas = document.getElementById('bg-canvas');
if (canvas) {
    const ctx = canvas.getContext('2d');
    let width, height;
    let particles = [];

    function resize() {
        width = canvas.width = window.innerWidth;
        height = canvas.height = window.innerHeight;
    }

    window.addEventListener('resize', resize);
    resize();

    class Particle {
        constructor() {
            this.reset();
        }

        reset() {
            this.x = Math.random() * width;
            this.y = Math.random() * height;
            this.vx = (Math.random() - 0.5) * 0.4;
            this.vy = (Math.random() - 0.5) * 0.4;
            this.radius = Math.random() * 1.5;
            this.alpha = Math.random() * 0.5 + 0.2;
        }

        update() {
            this.x += this.vx;
            this.y += this.vy;

            if (this.x < 0 || this.x > width || this.y < 0 || this.y > height) {
                this.reset();
            }
        }

        draw() {
            ctx.beginPath();
            ctx.arc(this.x, this.y, this.radius, 0, Math.PI * 2);
            ctx.fillStyle = `rgba(0, 240, 255, ${this.alpha})`;
            ctx.fill();
        }
    }

    for (let i = 0; i < 40; i++) {
        particles.push(new Particle());
    }

    function animate() {
        ctx.clearRect(0, 0, width, height);
        particles.forEach(p => {
            p.update();
            p.draw();
        });
        requestAnimationFrame(animate);
    }
    animate();
}

// 5. LIVE TERMINAL SIMULATOR
const terminalStream = document.getElementById('terminal-code-stream');
const codeSnippets = [
    "function initCore() { return new NeuralNet({ status: 'ACTIVE' }); }",
    "const btcValuation = await fetchCryptoData('BTC');",
    "security.enforceProtocol('2FA_MANDATORY');",
    "console.log('Command Center Ready v2026.0');"
];

if (terminalStream) {
    let snippetIdx = 0;
    setInterval(() => {
        terminalStream.innerText = codeSnippets[snippetIdx];
        snippetIdx = (snippetIdx + 1) % codeSnippets.length;
    }, 2500);
}

// 6. ANIMASI SALDO & STATS COUNTER
function animateCounters() {
    const balanceCounters = document.querySelectorAll('.balance-counter');
    const statCounters = document.querySelectorAll('.stat-counter');

    balanceCounters.forEach(counter => {
        const target = +counter.getAttribute('data-target');
        let current = 0;
        const increment = Math.ceil(target / 40);

        const update = () => {
            current += increment;
            if (current < target) {
                counter.innerText = 'Rp ' + current.toLocaleString('id-ID');
                setTimeout(update, 25);
            } else {
                counter.innerText = 'Rp ' + target.toLocaleString('id-ID');
            }
        };
        update();
    });

    statCounters.forEach(counter => {
        const target = +counter.getAttribute('data-target');
        let current = 0;
        const increment = Math.ceil(target / 30);

        const update = () => {
            current += increment;
            if (current < target) {
                counter.innerText = current;
                setTimeout(update, 30);
            } else {
                counter.innerText = target;
            }
        };
        update();
    });
}

// Intersection Observer untuk animasi counter
const observerOptions = { threshold: 0.2 };
const assetsSection = document.getElementById('assets');

if (assetsSection) {
    const observer = new IntersectionObserver((entries, obs) => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                animateCounters();
                obs.unobserve(entry.target);
            }
        });
    }, observerOptions);

    observer.observe(assetsSection);
}
